export interface TutorAuthResponseDTO {
  id: number;
  nome: string;
  email: string;
  perfil: string;
  token: string;
}
export interface TutorSession {
  id: number;
  nome: string;
  email: string;
  perfil: string;
  token: string;
}
